cmake_minimum_required(VERSION 3.16)
set(EXTRA_COMPONENT_DIRS "../components" "../managed_components")
set(COMPONENTS main)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(voicekey_bringup)
