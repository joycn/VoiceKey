# BUG-001：Mute恢复异常及录音幅度过低

状态：Open / Deferred，用户要求后续修复；2026-09-18停止本轮调参。优先级：高，影响核心语音输入。

## 环境与复现

reSpeaker XVF3800 + XIAO ESP32S3，XMOS1.0.8 I2S master48k，XIAO单USB，Mac语音备忘录，VoiceKey麦克风16k单声道。最初反馈时运行已修复WakeNet启动崩溃的正式固件，应用SHA256 `d720d1436e4b42bfdc01cf9557b5919922e0b7eefe9ec48af279881f54acb5b3`，412304字节；用户确认播放输出正常。

1. 未静音录音。
2. 按Mute使其静音，再按一次解除。
3. 比较相同距离、音量和设备位置下的录音。

预期：静音期间不输出人声；解除后能及时正常录音，无长时间极低音量，不影响播放。

实际：最初描述红灯持续亮、无法恢复；后续用户澄清独立红灯能熄灭，但人声极弱。后来静音前后都偏小，固定设备连续说话可逐渐变大。AGC门限候选实测用户报告完全听不到人声，判为失败。不同阶段现象不能混为已确定的单一根因。

## 已有证据

- 用户提供相近条件的两份录音：首份RMS约−27.79dBFS，静音恢复后样本约−67.41dBFS，相差约39.62dB；这不是严格同步的实验。见[录音分析](../device-records/2026-09-18-recording-analysis.md)。
- 最初HID无法打开，之后共享访问可读，发现控制通信失败/缓存过期会保护性关闭采集。见[静音调查](../device-records/2026-09-18-mute-recovery.md)及[控制实验](../device-records/2026-09-18-i2c-turnaround.md)。
- 只读诊断中MIC_GAIN保持90；AGCGAIN曾低于0.05，随后回升，用户听感逐渐变大。变化先于部分Mute操作；不证明AGC是唯一原因。见[增益诊断](../device-records/2026-09-18-gain-diagnostics.md)。
- 用户明确确认没有播放声音；USB播放缓存有数据不等于音箱发声。
- PP_AGCALPHAFASTGAIN=1.0候选软件检查通过但声学失败，已撤回。见[候选失败记录](../device-records/2026-09-18-agc-recovery-candidate.md)。

## 当前处理

用户要求代码恢复到首次提出静音恢复问题时的状态。保留此前硬件适配、播放、时钟保护及WakeNet启动修复；撤回后续I2C等待/退避、分散轮询、增益诊断扩展、AGC调参及主机共享打开/持续采样工具。完整撤回前源码和证据备份：`/Users/joy/.local/share/voicekey/backups/pre-mute-baseline-restore/source-and-evidence.tar.gz`。

该历史时点没有独立Git提交，当前按记录重建行为基线；不能未经哈希比较就声称逐字节恢复历史镜像。设备没有随代码回退：最后确认仍运行失败候选cb5bf7c5…，需另行安排烧录；候选未保存XMOS配置，日后设备回退需整板断电清除临时参数。

## 后续修复与验收

先对齐原始I2S、滤波后PCM、USB录音及主机所选输入，采集峰值/RMS/削波率，隔离信号源与DSP/格式/主机因素；同时复核控制故障及Mute状态。禁止仅凭LED、枚举成功或AGC数值宣称恢复。

修复后应在固定距离/音量条件下重复至少三次Mute循环，记录解除后的可用语音恢复时间；检查低声/正常语音、噪声、削波、播放并行、故障恢复及长时间运行。目标声学指标在修复前明确，现有远场/AEC/目标唤醒词验收继续保持未完成。

## 本次代码恢复验证

完整scripts/test.sh、交叉编译、8MB镜像校验、OpenSpec严格校验及git diff --check通过。恢复构建应用412288字节，SHA256 `a2449dfd4eeeea757926980e3c9f8d3eca1ddbe85b91b2f52b17f07a2e98ce46`；ELF SHA256 `694c4058de2271d6137ff33610c86692fca77934a481b808d40e1c0763f89982`。

该产物与历史d720d143…哈希及大小不同：这是依据留存记录撤回后续改动的行为基线重建，不是逐字节恢复的历史快照。尚未进行设备复测，原Bug明确保留。测试/构建日志保存在docs/device-records/logs/2026-09-18-first-mute-restore-*.log。本次未烧录。

设备状态更新：用户随后要求使用Hi, Joy及⇧⌥⌘S并烧录，已写入6898998e…应用及新模型，取代失败AGC候选。当前应用不写AGC门限，初始I2C错误仍出现。BUG维持Deferred；历史“仍运行失败候选”仅适用于回退代码时点。

## Hi Joy无法触发的关联证据

用户确认：喊Hi Joy没有蓝灯，Mac手动⇧⌥⌘S有效。当前运行8d654d50e73b5f5d（Hi Joy构建）且wake初始化成功。约10秒51快照：I2C错误增25，USB缺样增875，6个快照板状态无效并保护性静音。代码在静音状态变化时清空采集/唤醒队列、取消待发送事件、递增epoch；WakeNet下一段音频会重建上下文。这会破坏唤醒词连续音频，构成当前无法可靠检测的明确阻碍，但没有原始检测/触发计数，不能证明是唯一原因或排除LED更新失败。手动快捷键有效仅验证主机快捷键配置，不证明设备HID成功发送。

原始采样：docs/device-records/logs/2026-09-18-hi-joy-trigger-check.json。遵循用户暂缓BUG修复的要求，本次只读诊断与记录，不恢复已撤回的控制/AGC改动。后续若恢复处理，应先仅验证控制稳定性，再独立评估PCM及模型；不把调增益与控制修复混在一次实验中。

## 你好小智再次排查

当前运行fdc955a1875daef7，wake初始化成功，未物理静音，I2S约48kHz，HID键盘集合枚举正常。约10秒51快照内I2C错误增加24、busy增加834、USB缺样增加837，3个快照无效并保护性静音。控制故障会清空WakeNet队列、变更epoch、取消待发触发并清除检测灯窗口；这构成连续识别的明确阻碍。未暴露检测/触发计数，因此不能仅凭这些状态认定没有任何WAKENET_DETECTED或排除HID投递问题。请求运行态短按B绕过识别，并临时仅监听VoiceKey键盘报告；不修改增益或固件。

60秒VoiceKey专用HID监听结束，未采到报告；尚无用户确认在此窗口内按B，故不能据此认定USB发送故障或主机拦截。需用户完成并反馈绕过识别测试，必要时重新同步采样。

唤醒extension2实测进一步定位：90秒只检测1次、USB接受按下1次、抑制0次，后续Input Report全零，推理仍增长；同窗I2C错误207、wake epoch变化166。详见[唤醒链路证据](../device-records/2026-09-19-wake-path-diagnostics.md)。不能将“只触发一次”归因于未释放快捷键。

2026-09-19：用户明确要求继续修复。控制通信/连续识别部分恢复为In progress；AGC/录音声学部分仍不调参。仅恢复有界等待/退避与分散轮询，保留诊断计数，待软件及硬件验证。

控制修复版已烧录并核对运行身份；30秒内控制错误和epoch增量均0，推理937次。控制稳定性初步改善，连续口述及低音量/静音恢复验收未完成，BUG不关闭。[实测记录](../device-records/2026-09-19-control-repair-wake.md)。

### 2026-09-19：重新开展录音问题调查

用户明确要求优先继续处理，并再次确认当前复现条件为“按过静音之后恢复它就会变小”。这属于用户观察，尚未由同一次受控录音量化。只读设备检查确认应用 ELF 前缀 0853e1538b0e05ed、未静音、采集门开启、I2C 错误 0、采集故障代次 0、传输故障 0、I2S 约 48 kHz。瞬时 PCM 峰值不能替代定时人声录音，也不能据此锁定 AGC 根因。

恢复可复用的主机诊断入口：macOS 共享打开键盘 HID，仅唯一匹配时连接；不支持共享打开时拒绝回退独占；支持有界多样本 JSONL。已用真实设备单次读取验证，并测试共享打开顺序、失败关闭和多设备拒绝。没有修改设备 AGC、没有烧录固件。

下一步用固定位置、相同距离/音量的静音前后连续录音，比较恢复后立即、10 秒、30 秒的人声，并对照静音/采集门/故障计数。另做轻触设备但不切换静音的对照，避免把触碰冲击导致的增益变化误认为静音逻辑错误。

代码核查和新增回归：`vk_set_mute` 仅切换采集状态、失效旧音频和推理；采集任务在代次变化时清理 FIR/RX 队列，固定 PCM 增益没有静音后调低分支。实际 audio_input.c/runtime.c 适配测试新增静音前后相同非零输入块的逐样本幅度比较，验证通过；完整 test-adapters.sh 通过。该证据排除了测试覆盖条件下 XIAO 恢复逻辑对同一 PCM 的持续幅度衰减，不涵盖实际 XMOS 输出、机械冲击、I2S 有效位或 Mac 录音处理。

重新核对官方控制表：AGC 当前增益、快衰减时间常数和快模式阈值是不同参数；不能将门限误作恢复时间，也不能把一次瞬时读数视为声学结果。来源：https://wiki.seeedstudio.com/respeaker_xvf_3800_i2c_list/ 。当前无 AGC 参数写入，待人工受控复测后决定进一步诊断方案。

用户随后反馈当前语音对话就是按静音再恢复后的状态。记录为本次采集可用、人声可以进入对话；语音转写不证明录音幅度正常，尚缺静音前后同条件录音量化。

补充只读AGC诊断候选：保持192字节HID报告，在空余字节加入当前增益及快照年龄，扩展版本4；每秒最多一次读取，不写AGC。过期、非法数值、读取失败均标记无效。真实板控制任务测试覆盖读取失败后安全状态保持及下次成功恢复，运行时测试覆盖过期隐藏，解析器兼容旧扩展。全套软件测试和OpenSpec校验已通过；该候选尚未烧录，不能作为Bug已修复的证据。

只读AGC候选已交叉编译及镜像/分区校验通过：应用 414304 字节，SHA-256 `169a1fdd168542e6d525d7422456a6d813d3f0beb64006d6fb93a40fbcea96be`；ELF SHA-256 `6983a56b368cdaf208de5318d31d6fc179c062a34d06f31dca396b47ed3f77f2`。构建和全套测试日志保存在 device-records/logs/2026-09-19-agc-readonly-*.log。等待进入下载模式后部署和读回验证。

### 用户补充：第三次静音后变小

用户明确纠正：“在第三次静音之后声音就会变得很小”，并非第三次唤醒。后续复现应覆盖至少三轮物理静音与解除，分别保留初始状态、每轮解除后的同条件语音幅度和只读AGC增益。尚不能推断每次必现、计数型固件错误或具体根因；需对齐实际静音状态变更时间。

用户进一步补充：第二次静音恢复后的声音已比第一次小，第三次明显变小；语音备忘录观察到声音变小，但当前聊天仍能收音/识别。不同应用的输入设备、处理和显示尺度尚未核对，因此聊天转写成功不作为录音幅度恢复的证据。

五段用户录音已量化：[五次静音前后分析](../device-records/2026-09-19-five-mute-recordings.md)。全段RMS分别-34.05、-30.23、-26.79、-39.05、-75.85dBFS。第三/第四次明显衰减，但前两次整体幅度更高，不支持简单的每次单调下降。未同步采集AGC，根因仍未确认；不将聊天转写成功计为幅度验收通过。

新同步证据：[四轮静音录音与AGC对照](../device-records/2026-09-19-four-mute-correlation.md)。第一轮解除后约1秒AGC从3.25降至0.045，后续语音窗口幅度同时下降；不是第三次才首次发生。控制及采集故障计数不增，四次恢复边界合计USB缺样411。触发瞬态来源尚未确定，不宣称已修复。

用户对照反馈：单纯轻触外壳，声音没有变小。此前已说明重新断电开机并录音；轻触动作尚无精确时间标记，不能把任意遥测窗口直接当作动作对应结果。该反馈不支持普通轻触必然触发衰减；重点继续核对静音解除瞬间，按键动作与静音内部行为尚未独立隔离。等待断电后连续录音。

[断电后再次复现](../device-records/2026-09-19-powercycle-mute-correlation.md)：一次解除静音后AGC2.64→0.074（约-31dB）；新录音包含静音后满幅瞬态。普通轻触无复现。强瞬态来源待查，未修复；不要依据全段RMS -24.48dBFS认定语音音量正常。

用户补充：解除静音时LED明显像重置。代码确实会在静音切换时重新写颜色/速度/效果，从红色返回白色呼吸；这可解释动画重新开始，但不能据此证明芯片重启。完整重启后采样中XIAO的I2S累计帧数连续递增、控制错误和采集故障未增加，没有观察到XIAO重启证据；尚无XMOS专用重启计数，不能彻底排除其内部变化。额外发现实际LED亮度127（配置30），第一次静音前曾读到效果5（应用目标通常1/2/3），需进一步检查XMOS内部灯效覆盖，不直接视为重启证明。

已准备[灯效亮度恢复候选](../device-records/2026-09-19-led-brightness-restore.md)：修复每次切换未恢复亮度30、效果写入发生在亮度之后的问题。没有修改AGC，不作为音量问题修复结论；需实机确认两者是否相关。

[灯效修正后仍有尖刺](../device-records/2026-09-19-led-mute-spikes.md)：用户四轮录音多处静音边界附近出现接近满幅瞬态，后段持续低幅。HID采样提前读取失败且未捕捉切换，禁止假定精确同步或宣称全程无通信故障。问题未修复。
