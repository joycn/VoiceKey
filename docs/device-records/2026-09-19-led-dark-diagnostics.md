# 灯环完全不亮：只读诊断

用户确认能唤醒，但灯环完全不亮。保留白色15失败回退5、彩虹3、亮度10，不再盲调参数。通过现有安全GPIO读数保留X0D33灯环供电状态，每250ms最多增加一次只读LED参数查询，按effect/brightness/speed/gamma顺序形成快照；状态切换、查询失败作废，完整快照超过2秒不再报告有效。诊断失败不直接改静音或GPIO，也不自动恢复灯环供电。

HID schema3长度仍192，extension从2到3。XMOS build文字缩短至32字节（124–155）；156为LED快照有效/供电/速度回退标志，157–160为四项读回，161–164为快照最早读数年龄。174–191保持唤醒计数布局；脚本兼容extension2和3，老固件LED数据返回未知。现有已验证写入与静音100ms失效关闭机制保留。

当前设备仍运行f4ef3577版本；新增诊断尚未烧录。读回结果用于区分灯环电源关闭、配置异常、低亮度和长周期，不能仅凭单次累计控制错误认定已经回退速度。

完整测试与扩展板级快照测试通过，交叉编译/镜像/OpenSpec校验通过。应用414064字节，SHA256 `23dac94e44c114c74770a9edd7797415bdc7275b73c9bd614c7d0afea1bbc695`。尚未烧录，需ROM下载模式。
