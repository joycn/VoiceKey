# 呼吸/彩虹：速度1、亮度30

按用户指定，两种内置灯效均LED_SPEED=1、LED_BRIGHTNESS=30。删除白色15/5回退逻辑，保留extension3低频读回诊断、启动B防误触、唤醒彩虹5秒后恢复白色及静音/故障优先。灯环不亮根因尚未确认，本版需实机检查实际参数和供电。

适配器测试、交叉编译、镜像校验、OpenSpec严格校验通过。应用413968字节，SHA25620391d22b76d36a89c300e233a74f2107cbb36b42b79ae1e424922838897cb31。尚未烧录，设备当前仍为此前f4ef3577…版本，待ROM下载模式。

## 实机烧录与参数读回

MAC68:ee:8f:46:c6:2c核对，完整四项Hash验证成功后watchdog重启。运行ELF前缀0853e1538b0e05ed匹配；board/audio/wake初始化错误均0，wake_ready=true。extension3读回：LED供电开启、effect1、speed1、brightness30、gamma1、fallback=false，快照有效；首次控制错误0。上述为实际寄存器读回，不等同视觉验收，仍等待用户确认。

30秒151快照：控制错误和wake epoch增量均0，推理940次，检测/USB接受按下各1次。有效LED快照组合(effect,speed,brightness)：[(1, 1, 30), (2, 1, 30)]。视觉验收待用户反馈。
