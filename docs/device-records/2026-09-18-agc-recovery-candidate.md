# AGC低增益恢复候选修复

状态：实机验收失败，用户报告完全听不到人声。候选参数写入已从当前源码撤回；设备回退待完成。

依据前一轮遥测与用户确认的逐渐变大现象，启动/控制重新配置时写入并读回PP_AGCALPHAFASTGAIN(17,16)=1.0。只在低于单位增益时选择XMOS已有的快速恢复机制。此门限是项目候选值，不是官方推荐预设；尚不能认定机械冲击是根因。保留固定MIC_GAIN、当前AGCGAIN、目标功率、上限与所有时间常数。正常轮询不增加事务，不写物理静音，也不持久化XMOS配置。

官方依据：[XMOS AGC Configuration](https://www.xmos.com/documentation/XM-014888-PC/html/modules/fwk_xvf/doc/user_guide/04_tuning_the_application.html)。该文解释低于快速门限时的恢复路径，同时提醒修改时间常数可能导致语音音量波动。实际DSP行为仍以Seeed1.0.8固件实测为准。

验证：完整scripts/test.sh通过；新增控制测试验证写失败、读回不匹配均关闭采集，以及物理静音权威和解除时不重置实时增益。交叉编译/8MB镜像校验/OpenSpec严格校验通过。主机测试不模拟XMOS内部AGC，不能证明恢复速度。

应用413376字节，SHA256 `cb5bf7c56f1a80b6ae5a7b45d0fc3b37baec73ffebfeb2d337bf199991a61ed1`。ELF SHA256 `34f01afbe312da3dbc04e20b5e4c619f8d9cd97df79f11e3bf439c8df0a1d82f`。

待实机：身份核对、控制稳定性、相同距离连续说话及Mute前后幅度恢复；比较恢复时间、正常/小声语音、背景噪声、削波与音量起伏。没有上述证据不标记修复完成。

回退：移除该候选写入，构建并烧录此前实现，再断电重启两个芯片，以恢复XMOS默认易失配置。仅复位XIAO不能清除仍有供电的XMOS设置；本候选不执行SAVE_CONFIGURATION。

烧录：ROM识别MAC 68:ee:8f:46:c6:2c，镜像边界/哈希再次校验后四项写入校验通过，watchdog reset后HID恢复。运行ELF前缀34f01afbe312da3d与构建一致；board_valid=true意味着本候选启动时17/16=1.0的精确读回已通过（非独立实时参数遥测）。初始I2C错误、USB缺样/丢样与音频故障均0；未静音，采集门控开放。正在进行人工对比，不能由初始AGCGAIN约32推断修复成功。

120秒试验：121个快照全部板状态有效，11个为物理静音；I2C错误、采集/传输故障增量0，USB缺样342、播放欠载2415。不能描述为零缺样，也不能凭这些计数判定声学效果。用户录音反馈待补，7.4保持未完成。

## 实测失败与撤回

用户反馈完全听不到人声。同期诊断未静音、采集门控开放、I2S约48k、I2C错误0，AGCGAIN约0.645。这些状态不能证明有正常语音PCM，也不足以确认AGC是唯一根因。停止候选实验，撤回17/16=1.0设置，恢复只读增益诊断实现。回退应用后整板断电以清除XMOS临时参数，不使用未经验证的新增益方案替代。下一步应比较实际PCM/录音幅度，区分信号源、后处理、转换与主机录音链路。

回退准备完成：完整测试、构建、镜像校验与OpenSpec严格校验通过。应用SHA256恢复为08beebe8fa8bf23f49f96c93aff6f9c56cca0e0f1fad8c5e6fc934e3e06d3c91，ELF为f9a5997b80502c8097353496fb84a18ff14c9c1fcd569abc6e94ef4c48a7bc6f，与候选前完全一致。当前无ROM串口，已请求用户整板断电并按B重连进入下载模式；尚未回退设备。
