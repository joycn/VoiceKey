# I²C控制等待修复候选

现场已确认控制错误持续增长，稳定I²S约48kHz时仍会触发保护性关闭。原transport写请求后立即读，busy重试无调度等待。

board.c现对每次成功发送命令后等待2tick（配置1000Hz），再读取响应；设置写入也等待后才发读回请求。三次尝试、驱动10ms超时、100ms缓存过期关闭不变。此等待值是待实机验证的工程选择，非官方保证。没有更改静音输出、增益、音频格式或XMOS镜像。

完整scripts/test.sh通过，实际板任务测试覆盖延迟后可读、两次busy后成功以及原持续LED故障关闭。正式交叉编译与镜像校验通过，OpenSpec严格校验通过。

新应用412320字节，SHA256 `4707831d54e045df0a1c06d881893d0512eb9c4a74e9e5a61b34714f34434605`；其余三个镜像哈希见构建日志。尚未烧录，等待用户进入下载模式。当前设备仍运行旧固件，静音间歇问题、低音量和实机控制稳定性未完成验收。

日志：logs/2026-09-18-i2c-settle-tests.log、logs/2026-09-18-i2c-settle-build.log、logs/2026-09-18-before-i2c-repair.json。

## 首轮实机复测

后来发现/dev/cu.usbmodem144101并用esptool核对MAC68:ee:8f:46:c6:2c，烧录4707831d...修复候选，四项写入Hash verified，watchdog reset。HID应用ELF前缀757fe7baeb389db8，音频/模型启动正常。启动约4秒即报告8次错误、348次busy，2ms等待不足以消除问题，不能宣称修复。

正在构建第二轮：保留命令2tick等待，仅在busy且还有尝试次数时增加10ms任务退避，最多两次。与官方Python控制工具的busy循环10ms等待方向一致（其传输为USB，不将其视为I2C完成时间保证）。三个尝试用尽仍失败关闭，不延长缓存100ms限制。

首轮60秒结果：61快照、9次采样控制无效；errors增96、busy增4514、USB missing增6310；transport/capture faults无增长，时钟47998–48000Hz。第一轮未通过控制稳定性验收。原始证据logs/2026-09-18-i2c-settle-60s.jsonl。

第二轮完整test.sh/正式交叉构建/镜像校验/OpenSpec通过。应用412368字节，SHA256 `95a3db192079e8cb1542ad1aacbe394016e90e55cf39ffaa5dc458b577979f47`，尚未烧录。等待用户再次进入下载模式，当前运行第一轮4707831d...候选。

第二轮已用同一MAC核验后烧录，四项Hash verified；watchdog reset。运行HID ELF前缀8163abdd29c93864，版本/格式/初始化正确。初期errors=0而busy正常增加；正进行60秒观察及用户三轮静音/录音测试，不能提前标记全部通过。

第二轮60秒：I2C errors增量0，busy增3353（均通过重试）；61快照中3次board_valid=false，cache_age分别102/147/113ms，profile_error均ok且XMOS未静音。这说明通信错误改善，但集中低频读造成缓存过期，仍未通过持续采集验收。USB missing增4316，时钟47998–48000Hz且transport faults无增长。

第三轮候选：运行中每周期最多读取一个低频参数（路由轮流验证、AEC三项分周期并在整组完成后发布）。随后读取静音/I2S状态，使用这次安全状态读取之前的实际时钟作为缓存时间，避免把前面的慢操作时间误当静音采样时间。初始化仍要求完整配置/验证，失败仍关闭；100ms规则未变。带时间模型的真实协议测试模拟每次事务15ms，验证稳态最多三次读取与两次发布间的缓存年龄不超过95ms；这只是软件调度模型，不代替硬件复测。

第三轮完整软件测试、正式构建、镜像边界与OpenSpec验证通过；应用412800字节，SHA256 `14d4181200bb1f31e4287abf12934b79d70c4bd7ce352b0c8adf1b8bce368173`。未烧录；设备当前仍为第二轮95a3db19...，等用户三轮静音反馈并再次进入下载模式。

第三轮追加回归：AEC首项新值在整组完成前不覆盖旧完整快照；第二项失败后快照无效、采集关闭、待收集阶段清零；重新配置后仅发布完整新值。完整test.sh再次通过。固件源与构建镜像未改动，仍为14d41812...。后续检查下载口尚未出现，设备仍为第二轮ELF8163abdd29c93864且累计I2C错误0；三轮物理静音与录音反馈待用户提供。

第三轮已完成设备身份核验并烧录，四项Hash verified；运行ELF前缀5487469686c90ca5与本机ELF SHA256匹配。启动时I2C错误/USB缺样均0；一分钟观察进行中。

第三轮60秒完成：61快照全部board有效、时钟合格；I2C errors/USB missing/dropped/playback missing/dropped/capture faults/transport faults增量均0；busy增3387（通过重试完成）。时钟47998–48002Hz，采样cache_age最大50ms。Mac枚举16k单声道输入、48k双声道输出，均为默认设备（只读检查，未修改设置）。通过本轮有界控制稳定性检查；抽样不能证明不存在更短暂状态变化，且不等于长期/声学验收。等待当前固件的三轮Mute与录音质量反馈。证据logs/2026-09-18-control-spread-60s.jsonl。

用户反馈第三轮“每次不能恢复，声音仍很小”；随后即时HID读回board valid、physical muted=false、audio/streaming ready，I2C errors=0。用户体验仍失败，不能用先前60秒计数覆盖该反馈。正核对独立指示灯与录音应用/距离；需要同步状态与音频幅度证据，未修改增益或强制解除静音。证据logs/2026-09-18-third-user-failure.json。

用户澄清第三轮：独立红灯能熄灭，解除静音后录音偏小；使用macOS语音备忘录。因此先前“每次不能恢复”不再作为当前硬件静音无法解除的证据；解除静音已有用户反馈支持，低音量仍待排查。距离尚未提供。再次检查默认输入为VoiceKey16kmono，默认输出VoiceKey48kstereo；HID未静音、I2C errors0、时钟合格。需取得未经增强/归一化的短录音，量化峰值/RMS/静音占比，区分输入幅度与回放音量；不凭此诊断快照宣称音质通过。
