# 唤醒链路只读诊断

当前缺少原始识别计数，不能只凭无蓝灯判断模型是否识别。本轮添加extension2计数：完成推理、检测命中、wake epoch、USB已接受按下、被抑制的触发请求及最近推理块PCM绝对峰值。峰值500ms过期或epoch变化即无效；包含−32768的幅度32768边界已测试。不写AGC、不恢复控制修复、不改变模型或快捷键。

16位计数按65536取模，发送计数含BOOT开发触发且仅表示USB API接受报告，不能代表Mac目标应用执行。音频epoch变化包含保护性静音等原因，不等同模型错误。仅存统计，无原始音频。

完整test.sh与交叉构建/镜像校验通过。应用412768字节，SHA256 `028b019b17821263c27335446a06c79752bc0b87b767f6ea7ad64870943edcbd`。模型仍为你好小智56990e6b…。已完成下述烧录。后续需同一次设备运行对比安静、说词与短按BOOT三种阶段，读取计数差值，再决定修复方向；BUG-001仍未修复。

## 诊断版烧录

ROM读取MAC68:ee:8f:46:c6:2c，镜像验证后四项写入校验通过，watchdog reset。运行ELF前缀e6a958e76898e731匹配构建；wake初始化成功、extension2有效。首次报告推理296次、wake epoch24、检测0、HID按下0、抑制0，I2C错误30。epoch不是直接模型重建次数，但代码在已使用模型的epoch变化后重建上下文；初始epoch可能包含启动状态变化，必须比较稳定运行期间增量。正在采集说词与BOOT对照，不能提前判定唯一根因。

## 仅一次触发的现场结果

用户先报告语音和B能触发，随后澄清似乎只能触发一次。90秒451个快照：完成推理增加2529、检测命中增加1、USB已接受按下增加1、抑制请求增加0；音频wake epoch增加166、I2C错误增加207。唯一检测/按下变化约在采样4.819秒。无独立BOOT时间标注，不能声称语音和BOOT各自成功过一次。

随后的只读Input Report返回9个零字节（含报告ID），设备最后键盘状态已释放；检测总数和按下总数仍各1，推理总数继续增长。故没有证据支持按键未松开、冷却机制只允许一次或模型停止运行；未产生后续检测是本次计数直接支持的现象。控制错误和高频epoch变化是连续识别的明确阻碍，实际音量/模型准确性仍可能共同影响检测。

下一步建议单独恢复并验证控制等待/有限退避/分散低频查询，保留100ms失效关闭、当前模型/快捷键和这些计数；不混入AGC调参。此前用户要求暂缓的控制修复尚未在本轮重新应用。
